#ifndef WORLDTYPE_HPP
#define WORLDTYPE_HPP

namespace Editor {
    enum class WorldType {
        NonEditor       = 0,
        EditorProject   = 1,
        EditorTestLevel = 2,
    };
}

#endif //WORLDTYPE_HPP
